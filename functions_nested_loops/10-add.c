#include <stdio.h>
#include <math.h>
#include <ctype.h>
#include "main.h"

/**
 * add - Adds two values
 * Description: function that adds two ints and returns result
 * @x: int
 * @y: int
 * Return: Always 0 (success)
 */

int add(int x, int y)

{
	return (x + y);
}
